<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twilightzone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/oldagehomelogocopy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <activity android:name=".OldageHomeDetails"></activity>
        <activity android:name=".RegisterForPublic" />
        <activity android:name=".RegisterForSocialGroup" />
        <activity android:name=".RegisterForOldageHome" />
        <activity android:name=".Public" />
        <activity android:name=".Admin" />
        <activity android:name=".Government" />
        <activity android:name=".SocialGroups" />
        <activity android:name=".OldageHome" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>